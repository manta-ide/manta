<instruction>
I will apply the requested edits to the file and return only the complete, final file content. Follow these rules strictly:
- Apply edits exactly as specified using provided code context and markers.
- Preserve all unrelated code; maintain formatting and indentation.
- If context doesn’t match perfectly, prefer minimal, localized changes using token-level matching.
- Do not add explanations or markdown fences; output only the final file content.
Target path (optional): {{FILE_PATH}}
</instruction>

<code>{{FILE_CONTENT}}</code>

<update>{{PATCH_DESCRIPTION}}</update>
